/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: false,
    images: {
        domains: ['llibi-dms.sgp1.cdn.digitaloceanspaces.com']
    }
};

export default nextConfig;
